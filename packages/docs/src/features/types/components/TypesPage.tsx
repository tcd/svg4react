import { Page } from "@app/features/shared"
import { Box, Typography } from "@mui/material"

export const TypesPage = (_props: unknown): JSX.Element => {
    return (
        <Page title="Types">
        </Page>
    )
}
