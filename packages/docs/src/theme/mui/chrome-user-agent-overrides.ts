import type { SxProps } from "@mui/material"

export const ChromeUserAgentOverrides: Record<string, SxProps> = {
    h4: {
        display: "unset",
        marginBlockStart: "unset",
        marginBlockEnd: "unset",
        marginInlineStart: "unset",
        marginInlineEnd: "unset",
    },
}
