import type { ISxProps as SxProps } from "@app/theme/helpers"

const root: SxProps = {
    display: "flex",
}

const main: SxProps = {
    flexGrow: 1,
    p: 3,
}

export const Layout = {
    root,
    main,
}
